# Bot Framework — Teams integration
botbuilder-core==4.15.0
botbuilder-schema==4.15.0
botframework-connector==4.15.0
aiohttp>=3.9.0

# Gemini
google-genai>=2.23.0

# Excel parsing (for generate_csv.py build step)
openpyxl>=3.1.0

# Session storage — Upstash Redis (Vercel KV)
upstash-redis>=1.0.0
